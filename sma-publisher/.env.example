# Only needed for src/send_brevo.py. Rendering needs no environment variables.
BREVO_API_KEY=your-brevo-api-key

# Optional overrides (defaults live at the top of src/send_brevo.py)
BREVO_SENDER_EMAIL=verified-sender@example.com
BREVO_SENDER_NAME=SMA Israel Association
BREVO_LIST_ID=2
